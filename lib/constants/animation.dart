const Duration kOffsetDelay = Duration(milliseconds: 50);
const Duration kFadeDelay = Duration(milliseconds: 400);
const Duration kDotTranslateDuration = Duration(milliseconds: 350);
const Duration kPlaneGlowDuration = Duration(milliseconds: 300);
